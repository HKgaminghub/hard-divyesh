<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Order Confirmation</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f8f9fa;
            text-align: center;
            margin: 0;
            padding: 0;
        }
        h1 {
            color: #343a40;
            margin-top: 20px;
        }
        .order-details {
            background: white;
            width: 60%;
            max-width: 400px;
            margin: 20px auto;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1);
        }
        h2 {
            color: #007bff;
            margin-bottom: 10px;
        }
        p {
            font-size: 16px;
            color: #555;
        }
        a {
            display: inline-block;
            margin-top: 15px;
            padding: 10px 20px;
            text-decoration: none;
            color: white;
            background: #007bff;
            border-radius: 5px;
            transition: background 0.3s;
        }
        a:hover {
            background: #0056b3;
        }

        .order-details img {
            width: 100%;
            height: 200px;
            object-fit: cover;
        }

        button {
            display: inline-block;
            margin-top: 15px;
            padding: 10px 20px;
            text-decoration: none;
            color: white;
            background: #007bff;
            border-radius: 5px;
            transition: background 0.3s;
            border-color: #007bff;
        }

        button:hover{
            background: #0056b3;
        }
    </style>
</head>
<body>
    <h1>Order Confirmation</h1>

    <form action="/orderPlace" method="post">
    <div class="order-details">
            <h2>Product Details</h2>
            <img src="data:image/jpeg;base64,<%= image.toString('base64') %>" alt="Product Image">
            <p><strong>Product Name:</strong> <%= productName %></p>
            <p><strong>Price:</strong> ₹<%= buyingPrice %></p>
            <input type="hidden" name="productName" value="<%= productName%>">
            <div>
                <label>Address:-</label>
                <input type="text" name="Address" required>
            </div>
            <br>
            <div>
                <label>Quantity:-</label>
                <input type="Number" name="quantity" required>
            </div>
            <br>
            <div>
                <label for="phoneNumber">Phone Number:-</label>
                <input type="Number" name="phoneNumber" pattern="\d{10}" maxlength="10" required>
            </div>
    </div>
        <p><a href="/">Go Back to Home</a> &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; <button type="submit" >Place order</button> </button></p>
    </form>
</body>
</html>